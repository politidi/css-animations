version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.8
    working_directory: ~/repo
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "f8:06:81:36:fc:60:47:03:ac:af:c7:dc:23:28:5b:07"
      - run:
          name: Run tests
          command: |
            mkdir kek && cd kek && touch lol
            
workflows:
  sample:
    jobs:
      - build
